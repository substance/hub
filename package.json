{
  "name": "substance-hub",
  "description": "Experimental server for realtime collab",
  "dependencies": {
    "knex": "0.9.0",
    "lodash": "3.10.1",
    "sqlite3": "3.1.1",
    "ws": "0.8.1"
  },
  "devDependencies": {
  },
  "scripts": {
    "seed": "rm -f dev.hub.sqlite3 && knex migrate:latest && knex seed:run",
    "start": "node server.js"
  },
  "version": "0.1.0"
}
